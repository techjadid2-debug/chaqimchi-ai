# ENES Cloud kunlik backup — FAQAT BAZA.
#
# Ilgari backup FAQAT deploy paytida olinardi (scripts/deploy_cloud.sh).
# Uch hafta deploy bo'lmasa va VPS o'lsa — uch haftalik hodisa, hisob-faktura
# va akkauntlar yo'qolardi.  Bu unit backup'ni deploy'dan uzadi.
#
# Media (rasm/kliplar) bu yerda YO'Q — u haftalik `enes-backup-media`
# unitida.  Sabab: har kecha butun MinIO nusxalanib, arxiv 14 kun saqlanardi
# va media hajmi diskda ~15 barobar takrorlanardi.  96 GB server ikki-uch
# do'kondan keyin to'lardi.  Kunlik arxiv endi ~50-200 MB.
#
# O'rnatish (VPS'da, root):
#   cp deploy/backup.env.example /etc/enes/backup.env  # va tahrirlang
#   chmod 600 /etc/enes/backup.env
#   cp deploy/enes-backup.{service,timer} /etc/systemd/system/
#   cp deploy/enes-backup-failed.service /etc/systemd/system/
#   systemctl daemon-reload && systemctl enable --now enes-backup.timer
#   systemctl start enes-backup.service   # birinchi sinov darhol

[Unit]
Description=ENES Cloud kunlik backup (PostgreSQL + cloud-state)
Wants=docker.service
After=docker.service network-online.target
# Zaxira yiqilsa jim qolmasin — Telegramga xabar ketadi.
OnFailure=enes-backup-failed.service

[Service]
Type=oneshot
User=deploy
WorkingDirectory=/home/deploy/enes
EnvironmentFile=/etc/enes/backup.env
ExecStart=/home/deploy/enes/scripts/backup_production.sh
# 14 kundan eski BAZA arxivlari o'chiriladi.  Naqsh `enes-[0-9]*`:
# media arxivlari (`enes-media-*`) bu qoidaga tushmasin — ular
# o'z unitida, boshqa muddat bilan tozalanadi.
# Muhim nusxalarni restic/tashqi joyga ko'chirish backup skriptining o'zida
# (RESTIC_REPOSITORY berilsa) bajariladi.
ExecStartPost=/usr/bin/find ${ENES_BACKUP_DIR} -name enes-[0-9]*.tar.gz.enc -mtime +14 -delete
Nice=10
IOSchedulingClass=idle
