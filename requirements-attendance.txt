# Yopiq davomat piloti — faqat `CHAQIMCHI_ATTENDANCE_PILOT=true` bo'lganda
# o'rnatiladi (`scripts/install_sotqin.sh`).
#
# Tijoriy rejim alohida model manifesti tasdiqlanmaguncha fail-closed
# (`face.commercial_model_licensed: false`), shuning uchun bu paketlar
# standart Sotqin qutisiga umuman tushmaydi.
#
# Yo'l xaritasi: yuz tanish cloudga ko'chiriladi — edge faqat sifat
# tekshiruvidan o'tgan crop yuboradi, embedding va watchlist matching
# serverda bo'ladi.  O'shanda bu fayl butunlay olib tashlanadi.
-r requirements-sotqin.txt
insightface>=0.7.3,<0.8.0
onnx>=1.14.0,<2.0.0
onnxruntime>=1.16.0,<2.0.0
