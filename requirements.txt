# Chaqimchi AI — dev/CI to'plami.
#
# Mahsulot to'plamlari alohida:
#   qurilma (Linux Sotqin)  -> requirements-sotqin.txt
#   qurilma (Windows lokal) -> requirements-windows-local.txt (pinned)
#   cloud                   -> requirements-cloud.txt
#
# Yuz tanish kutubxonalari (insightface/onnx/onnxruntime) olib tashlangan:
# lokal davomat to'plami arxivlangan (`archive/attendance-local` tegi) va
# CI har push'da ~1 GB keraksiz paket o'rnatib o'tirmasligi kerak.
#
# Python: 3.12 — rasmiy qo'llab-quvvatlanadigan versiya (Docker va CI shu).
# Har bir bog'liqlikda yuqori chegara bor — buzuvchi major versiya jimgina
# kirib kelmasligi uchun.

numpy>=1.24.0,<3.0.0
opencv-python>=4.8.0,<5.0.0
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
PyYAML>=6.0.0,<7.0.0
pydantic>=2.5.0,<3.0.0
python-multipart>=0.0.9,<0.1.0
httpx>=0.25.0,<1.0.0
PyJWT>=2.8.0,<3.0.0
cryptography>=42.0.0,<49.0.0
# Production cloud (import faqat DATABASE_URL/S3 sozlanganda bajariladi)
psycopg[binary]>=3.2.0,<4.0.0
minio>=7.2.0,<8.0.0
